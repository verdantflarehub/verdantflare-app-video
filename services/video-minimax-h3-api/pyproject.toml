[project]
name = "verdantflare-video-minimax-h3-api"
version = "0.3.2"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools==78.1.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
# Service entrypoint wraps the upstream container runtime.
py-modules = []
